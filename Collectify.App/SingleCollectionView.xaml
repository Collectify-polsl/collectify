<Window x:Class="Collectify.App.SingleCollectionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Collectify.App"
        mc:Ignorable="d"
        Title="SingleCollectionView" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10">
            <Button Content="Add New Element"
            Command="{Binding AddNewEllementCommand}"
            Padding="10,5"
            Margin="0,0,10,0"/>
            <ComboBox
            Width="150"
            ItemsSource="{Binding CollectionList}"
            SelectedIndex = "0"
            >
            </ComboBox>
            <Button Content="Back to Main View"
                Command="{Binding ReturnCollectionsViewCommand}"
                Padding="10,5"
                Margin="10,0,10,0"
                />


        </StackPanel>

        <DataGrid Grid.Row="1"
              ItemsSource="{Binding DynamicTable}"
              AutoGenerateColumns="True"
              Margin="10">

        </DataGrid>

    </Grid>
</Window>
