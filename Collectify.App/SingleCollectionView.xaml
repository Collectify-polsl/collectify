<UserControl x:Class="Collectify.App.SingleCollectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Collectify.App"
             Background="#F0F2F5">

    <UserControl.Resources>
        <local:BytesToImageConverter x:Key="BytesToImageConverter"/>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <DataTemplate x:Key="ReferenceCellTemplate">
            <Button Content="..." 
                    Command="{Binding DataContext.NavigateToReferencedItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    CommandParameter="{Binding}"
                    Width="30" Height="22" Background="#E0F2FE" Foreground="#0369A1" 
                    FontWeight="Bold" BorderThickness="0" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </DataTemplate>

        <DataTemplate x:Key="ImageCellTemplate">
            <Button Command="{Binding DataContext.OpenFullImageCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
            CommandParameter="{Binding}"
            Background="Transparent" BorderThickness="0" Cursor="Hand">
                <TextBlock Text="View Image"/>
            </Button>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" BorderBrush="#D1D5DB" BorderThickness="0,0,0,1" Padding="15,10">
            <DockPanel LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="← Back" Command="{Binding ReturnCollectionsViewCommand}"
                            Style="{StaticResource ActionButtonStyle}" Background="#4B5563" Foreground="White" Margin="0,0,15,0"/>
                    <ComboBox Width="220" ItemsSource="{Binding CollectionList}" 
                              SelectedItem="{Binding SelectedCollectionItem}" DisplayMemberPath="Name"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="+ Add Item" Command="{Binding AddNewElementCommand}"
                            Style="{StaticResource ActionButtonStyle}" Background="#10B981" Foreground="White" Margin="0,0,10,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCollectionCommand}"
                            Style="{StaticResource ActionButtonStyle}" Background="#EF4444" Foreground="White"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Background="White" Margin="12" CornerRadius="8" BorderBrush="#D1D5DB" BorderThickness="1">

            <DataGrid ItemsSource="{Binding DynamicTable}"
            SelectedItem="{Binding SelectedRow}" AutoGenerateColumns="True"
            AutoGeneratingColumn="DataGrid_OnAutoGeneratingColumn"
            IsReadOnly="True" 
             RowHeight="45" 
            SelectionMode="Single"
            EnableRowVirtualization="False" HeadersVisibility="Column" 
            GridLinesVisibility="Horizontal"
                 HorizontalGridLinesBrush="#F3F4F6">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Background" Value="#F9FAFB"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>